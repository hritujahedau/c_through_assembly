

#include <stdio.h>

int num;

int main()
{
	num = 100;
	if(num == 20)
	{
		printf("\n in first if block num is 20");
	}
	if(num == 40)
	{
		printf("\n in second if block num is 40");
	}
	if(num == 60)
	{
		printf("\n in third if block num is 60");
	}
	if(num == 80)
	{
		printf("\n in forth if block num is 80");
	}
	if(num == 100)
	{
		printf("\n in fifth if block num is 100");
	}
	
	return 0;
}
